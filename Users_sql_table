CREATE TABLE Users (
    id int NOT NULL,
    username varchar(255) NOT NULL,
    password varchar(255) NOT NULL,
    file_storage int,
    processes int,
    PRIMARY KEY(id),
    UNIQUE (username)
);
